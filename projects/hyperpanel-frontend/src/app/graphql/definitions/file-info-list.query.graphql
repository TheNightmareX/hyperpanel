query FileInfoList($path: String!, $offset: Int, $limit: Int) {
  fileInfoList(path: $path, offset: $offset, limit: $limit) {
    total
    items {
      id
      name
      type
      size
      sizeFormatted
      modifiedAt
    }
  }
}
